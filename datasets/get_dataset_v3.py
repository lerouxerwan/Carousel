import numpy as np


def get_dataset_v3(N: int, K: float = 1, u_bar: float = 5, R: float= 10) -> tuple[np.ndarray, np.ndarray]:
    "Half of the data sample randomly everywhere, half sample centred on the circle"
    pass
